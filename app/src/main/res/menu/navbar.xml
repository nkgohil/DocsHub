<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    android:checkableBehavior="single">
    <group>
        <group  android:checkableBehavior="single">
            <item

                android:id="@+id/myappoint"
                android:title="My Appointments"
                android:icon="@mipmap/menuappoint" />
        </group>
        <group android:checkableBehavior="single">
        <item
            android:id="@+id/mydoc"
            android:title="My Doctor"
            android:icon="@mipmap/menudoc" />
        </group>
        <group android:checkableBehavior="single">
        <item
            android:id="@+id/healthblog"
            android:title="Health Blog"
            android:icon="@mipmap/menublog" />
        </group>
        <group android:checkableBehavior="single">
            <item
            android:id="@+id/setting"
            android:title="Settings"
            android:icon="@mipmap/menusetting" />
        </group>
        <group android:checkableBehavior="single">
            <item
            android:id="@+id/logout"
            android:title="Logout"
            android:icon="@mipmap/logout" />
        </group>
    </group>
</menu>